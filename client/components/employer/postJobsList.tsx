import { StyleSheet, Text, View } from "react-native";
import React from "react";

const postJobsList = () => {
  return (
    <View>
      <Text>postJobsList</Text>
    </View>
  );
};

export default postJobsList;

const styles = StyleSheet.create({});
